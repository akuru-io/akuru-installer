import Header from 'components/Header';

import {
    Layout,
} from './styled';

export default function VoteFonts(){
    const title ='Vote for font concepts'
    return(
        <Layout>
            <Header title={title} />
        </Layout>
    );
};